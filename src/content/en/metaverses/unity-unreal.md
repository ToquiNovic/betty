---
title: 3D Engines Integration (Unity / Unreal Engine)
description: Connecting game engines and metaverse simulations to Betty PaaS.
---

Publish simulated object telemetry from Unity (via `M2Mqtt` C#) or Unreal Engine (via the MQTT plugin) with `origin_type: "metaverso"` on topic `betty/sensor/:id/data`.
